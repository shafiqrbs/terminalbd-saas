{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SalesItem list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantity</th>
                <th>Salesprice</th>
                <th>Purchaseprice</th>
                <th>Estimateprice</th>
                <th>Customprice</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('salesitem_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.quantity }}</td>
                <td>{{ entity.salesPrice }}</td>
                <td>{{ entity.purchasePrice }}</td>
                <td>{{ entity.estimatePrice }}</td>
                <td>{{ entity.customPrice }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('salesitem_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('salesitem_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('salesitem_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
