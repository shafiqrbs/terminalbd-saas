{% if widget.jsFeature is not empty and widget.jsFeature.slug == 'slider' %}
    <div id="main-carousel" class="carousel slide"   data-pause="hover" data-ride="carousel" data-match-height="slider-height">
        <!-- Indicators -->
        <ol class="carousel-indicators">
            {% for slide in widget.featureWidgetItems %}
                <li data-target="#main-carousel" data-slide-to="{{ loop.index-1 }}" class="{% if loop.index - 1 == 0 %} active {% endif %}"></li>
            {% endfor %}
        </ol>
        <!-- Wrapper for slides -->
        <div class="carousel-inner">
            {% for slide in widget.featureWidgetItems %}

                {% set path ='' %}
                 {% if slide.feature.targetTo == 'Page' and slide.feature.menu is not empty %}
                    {% set path =  path(globalOption.subDomain ~ '_webservice_module',{'module':slide.feature.menu.slug})  %}
                 {% endif %}
                <div class="item {% if loop.index-1 == 0 %} active {% endif %}">
                    <a href="{{ path }}"><img class="image {% if widget.sliderFeature is empty %}  carousel-img {% else %} carousel-tab-img {% endif %}"  src="{% if slide.feature.webPath is not empty  %} {{ image(slide.feature.webPath) }}{% endif %}" alt="..."></a>
                    <div class="caption-{{ slide.feature.captionPosition }}" style="background-color: {{ slide.feature.captionBgColor }}!important; color: {{ slide.feature.captionFontColor }}!important;">
                        {% if slide.feature.name is not empty %}<h2 data-animation="animated flipInX" class="delay-2s"  data-wow-delay="2s" >{{ slide.feature.name|upper }}{% endif %}</h2>
                        {% if slide.feature.content is not empty %}<p data-animation="animated fadeInRight" class="delay-3s" data-wow-delay="3s" >{{ slide.feature.content|raw }}</p>{% endif %}
                        {% if slide.feature.targetTo == 'Page' %}
                            <a data-animation="animated fadeInLeft" class="btn btn-mini delay-4s" data-wow-delay="4s" style="background-color: {{ slide.feature.buttonBg }}" href="{{ path }}">{{ slide.feature.buttonName }}</a>
                        {% endif %}
                    </div>
                </div>
            {% endfor %}
        </div>
        <a class="carousel-main-control carousel-left" href="#main-carousel" data-slide="prev">‹</a>
        <a class="carousel-main-control carousel-right" href="#main-carousel" data-slide="next">›</a>
    </div>
    <div class="clearfix"></div>
    {% if globalOption.templateCustomize.siteTitle is not empty %}
        <div class="container wow fadeInDown grid" data-wow-delay="2s" >
            <div class="section-header" style="margin-top:60px">
                <{{ globalOption.templateCustomize.siteTitleSize }}  class="animated fadeInLeftBig slide-delay-1" {% if globalOption.templateCustomize.siteTitleColor is not empty %} style="color:{{ globalOption.templateCustomize.siteTitleColor }}!important" {% endif %} >{{ globalOption.templateCustomize.siteTitle }}</{{ globalOption.templateCustomize.siteTitleSize }}>
            {% if globalOption.templateCustomize.siteSlogan is not empty %}
                <p class="animated fadeInUpBig slide-delay-3" data-wow-delay="1s" >{{  globalOption.templateCustomize.siteSlogan }}</p>
            {% endif %}
            </div>
    {% endif %}

{% endif %}
    {% if widget.jsFeature is not empty and widget.jsFeature.slug == 'top-tab-slider' %}
        <div class="container">
            <div class="row top-tab-slider">
                <div class="col-md-12">

                    <div id="topTabCarousel" class="carousel slide" data-match-height="slider-height"  data-ride="carousel" >
                        <ul class="nav nav-pills nav-justified">
                            {% for slide in widget.featureWidgetItems %}
                                <li data-target="#topTabCarousel" style="background-color: {{ slide.feature.captionBgColor }}" data-slide-to="{{ loop.index-1 }}" class="{% if loop.index-1 == 0 %} active {% endif %}"><a href="#">{{ slide.feature.name|truncate(20, false) }}<small>{{ slide.feature.content|truncate(40, false) }}</small></a></li>
                            {% endfor %}
                        </ul>
                        <!-- Wrapper for slides -->
                        <div class="carousel-inner">
                            {% for slide in widget.featureWidgetItems %}
                                <div class="item {% if loop.index-1 == 0 %} active {% endif %}">
                                    <img class="image {% if widget.sliderFeature is empty %}  carousel-img {% else %} carousel-tab-img {% endif %}" src="{% if slide.feature.webPath is not empty  %} {{ image(slide.feature.webPath).resize(1366,768) }}{% endif %}"  alt="...">
                                    <div class="carousel-caption" >
                                        {% if slide.feature.targetTo == 'Page' and slide.feature.menu is not empty %}
                                            <a class="btn btn-mini " style="background-color: {{ slide.feature.buttonBg }}" href="{{ path(globalOption.subDomain ~ '_webservice_module',{'slug':slide.feature.menu.slug}) }}">{{ slide.feature.buttonName }}</a>
                                        {% endif %}
                                    </div>
                                </div>
                            {% endfor %}
                            <!-- End Item -->
                        </div>
                        <!-- End Carousel Inner -->
                    </div>
                </div>
                <!-- End Carousel -->
            </div>
            <div class="clearfix"></div>
        </div>
        <div class="clearfix"></div>
        {% if globalOption.templateCustomize.siteTitle is not empty %}
            <div class="container wow fadeInDown grid" data-wow-delay="2s" >
                <div class="section-header" style="margin-top:60px">
                    <{{ globalOption.templateCustomize.siteTitleSize }}  class="animated fadeInLeftBig slide-delay-1" {% if globalOption.templateCustomize.siteTitleColor is not empty %} style="color:{{ globalOption.templateCustomize.siteTitleColor }}!important" {% endif %} >{{ globalOption.templateCustomize.siteTitle }}</{{ globalOption.templateCustomize.siteTitleSize }}>
                {% if globalOption.templateCustomize.siteSlogan is not empty %}
                    <p  data-animation="animated fadeInUpBig" class="delay-1s" >{{  globalOption.templateCustomize.siteSlogan }}</p>
                {% endif %}
            </div>

        {% endif %}
    {% endif %}
    {% if widget.jsFeature is not empty and widget.jsFeature.slug == 'bottom-tab-slider' %}
                <div class="container">
        <div class="row bottom-tab-slider" >
            <div class="col-md-12" >
                <div id="bottomTabCarousel" class="carousel slide" data-match-height="slider-height"  data-ride="carousel">
                    <!-- Wrapper for slides -->
                    <div class="carousel-inner">
                        {% for slide in widget.featureWidgetItems %}
                            <div class="item {% if loop.index-1 == 0 %} active {% endif %}">
                                <img class="image {% if widget.sliderFeature is empty %}  carousel-img {% else %} carousel-tab-img {% endif %}"  src="{% if slide.feature.webPath is not empty  %} {{ image(slide.feature.webPath).resize(1366,768) }}{% endif %}" alt="...">
                                <div class="carousel-caption">
                                    {% if slide.feature.targetTo == 'Page' and slide.feature.menu is not empty %}
                                        <a data-animation="animated fadeInUpBig" class="delay-1s" class="btn btn-mini " style="background-color: {{ slide.feature.buttonBg }}" href="{{ path(globalOption.subDomain ~ '_webservice_module',{'module':slide.feature.menu.slug}) }}">{{ slide.feature.buttonName }}</a>
                                    {% endif %}
                                </div>

                            </div>
                        {% endfor %}
                    </div>
                    <ul class="nav nav-pills nav-justified tab-nav">
                        {% for slide in widget.featureWidgetItems %}
                            <li data-target="#bottomTabCarousel" style="background-color: {{ slide.feature.captionBgColor }}" data-slide-to="{{ loop.index-1 }}" class="{% if loop.index-1 == 0 %} active {% endif %}"><a href="#">{{ slide.feature.name|truncate(20, false) }}<small>{{ slide.feature.content|truncate(40, false) }}</small></a></li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
            <!-- End Carousel Inner -->
        </div>
    </div>
    <div class="clearfix"></div>
        {% if globalOption.templateCustomize.siteTitle is not empty %}
            <div class="container wow fadeInDown grid" data-wow-delay="2s" >
                <div class="section-header" style="margin-top:60px">
                    <{{ globalOption.templateCustomize.siteTitleSize }}  class="animated fadeInLeftBig slide-delay-1" {% if globalOption.templateCustomize.siteTitleColor is not empty %} style="color:{{ globalOption.templateCustomize.siteTitleColor }}!important" {% endif %} >{{ globalOption.templateCustomize.siteTitle }}</{{ globalOption.templateCustomize.siteTitleSize }}>
                {% if globalOption.templateCustomize.siteSlogan is not empty %}
                    <p data-animation="animated fadeInUpBig" class="delay-1s">{{  globalOption.templateCustomize.siteSlogan }}</p>
                {% endif %}
            </div>
        {% endif %}
    {% endif %}
    {% if widget.jsFeature is not empty and widget.jsFeature.slug == 'feature' %}
        <div class="container">
        <div class="wow fadeInDown" data-wow-delay="1s">
            <div class="row">
                {% for slide in widget.featureWidgetItems %}
                    {% if(slide.feature.webPath) %}
                        {% set path ='' %}
                        {% if slide.feature.targetTo == 'Page' and slide.feature.menu is not empty %}
                            {% set path =  path(globalOption.subDomain ~ '_webservice_module',{'module':slide.feature.menu.slug})  %}
                        {% endif %}
                        <div  style="overflow: hidden;height: {{ slide.divHeight }}px; width: {{ slide.divWidth }}%; margin-bottom: 15px; position: relative"  class="col-xs-12 col-md-12 feature-box ">
                            <div class="box-img">
                                {%  set imageWidth =  slide.divWidth~'%' %}
                                <img class="img-responsive img-responsive-feature" style="height:{{ slide.divHeight }}px;"   src="{{ image(slide.feature.webPath) }}" alt="">
                                <span class="img-responsive-preview img-responsive-preview-{{  slide.feature.captionPosition }} " style="background-color: {{ slide.feature.captionBgColor }};color: {{ slide.feature.buttonBg }}; " >{{ slide.feature.name }}</span>
                            </div>
                        </div>
                    {% endif %}
                {% endfor %}
            </div>
        </div>
        </div>
    {% endif %}
    <div class="container">
        {% if widget.content is not empty %}
            <div class="clearfix"><br/></div>
            <div class="row grid wow fadeInDown" data-wow-delay="2s"> <div class="col-md-12 text-justify">{{ widget.content|raw }}</div></div>
            <div class="clearfix"><br/></div>
        {% endif %}
    </div>